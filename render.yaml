databases:
  - name: db
    postgresMajorVersion: '18'
    plan: free
    region: frankfurt

services:
  - type: web
    name: my-web-service
    env: node
    plan: free
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true